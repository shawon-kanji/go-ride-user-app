---
phase: 01-foundation-auth
plan: 08
type: execute
wave: 8
depends_on: ["01-07"]
files_modified:
  - .planning/phases/01-foundation-auth/01-08-SUMMARY.md
  - .env.example
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]
user_setup:
  - service: google-cloud-maps
    why: "The shared Google Maps API key is restricted by Android package name + signing-certificate SHA-1. This app's package (com.goride.rider) and this machine's debug keystore fingerprint are not registered yet, so the key would be rejected the first time Phase 3 renders a map."
    env_vars:
      - name: GOOGLE_MAPS_API_KEY
        source: "Already set in .env during plan 01-01, copied from ../go-ride-driver-app/.env (MAP_API_KEY). No new key needed."
    dashboard_config:
      - task: "Add an Android app restriction entry: package name com.goride.rider + the local debug keystore SHA-1 printed by Task 1"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> (the existing Maps API key) -> Application restrictions -> Android apps"

must_haves:
  truths:
    - "The built Android app carries a real Google Maps API key in its manifest, not the literal string 'process.env.GOOGLE_MAPS_API_KEY' and not an empty value"
    - "The local debug keystore's SHA-1 is registered against that key alongside package com.goride.rider, so the key resolves in a local debug build"
    - "A rider can complete the whole phase on a real Android build: sign up, get into the app, force-quit and reopen and still be logged in, view and edit profile, change password, and log out"
    - "The full Jest suite and TypeScript type-check are green at the phase gate"
  artifacts:
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Generated manifest carrying com.google.android.geo.API_KEY (gitignored build output — existence is verified, not committed)"
      contains: "com.google.android.geo.API_KEY"
  key_links:
    - from: "app.config.js"
      to: "android/app/src/main/AndroidManifest.xml"
      via: "react-native-maps config plugin writing androidGoogleMapsApiKey during prebuild"
      pattern: "com\\.google\\.android\\.geo\\.API_KEY"
---

<objective>
Close the phase against reality: run the full automated gate, generate the native Android project, prove the Google Maps API key resolves to a real value in the built manifest, register this machine's debug-keystore SHA-1 against the key, and have a human walk the complete AUTH-01..06 flow on a real Android build.

Purpose: `01-VALIDATION.md` § Manual-Only Verifications lists two things mocked unit tests genuinely cannot prove — session persistence across a real app restart (AUTH-02's whole point) and that the Maps key survives the config→manifest→APK path. `01-RESEARCH.md` Pitfall 3 and Pitfall 4 are both failures that only surface at this step, and both are cheap here and expensive in Phase 3.
Output: A verified local Android build, a registered SHA-1, and a human sign-off on the full rider auth journey.
</objective>

<execution_context>
@/Users/shawonkanji/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawonkanji/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-VALIDATION.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-07-SUMMARY.md
@.planning/ROADMAP.md

<environment_facts>
Verified on this machine on 2026-08-11 — do not re-discover, but DO re-check the device state (it changes):
- JDK: OpenJDK 17.0.20 (Homebrew) — present, sufficient for `expo run:android`
- `ANDROID_HOME` = `/usr/local/share/android-commandlinetools`; platforms `android-35`/`android-36`, build-tools `35.0.0`/`36.0.0` present
- `adb`, `sdkmanager`, `avdmanager`, `keytool` are all on PATH
- **No `emulator` binary and zero system-images installed** — a fresh AVD is a multi-hundred-MB download
- `adb devices` listed NO attached device at planning time
- **No `gcloud` CLI installed** — the API-key restriction cannot be scripted; it must be done in the web console
- Builds are LOCAL ONLY (`npx expo run:android`). There is no EAS project, no `eas.json`, and no `extra.eas.projectId` in `app.config.js`. Ignore any EAS-flavoured wording in `.planning/research/PITFALLS.md` and `.planning/STATE.md` — `01-RESEARCH.md` supersedes it.
</environment_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — full suite, type-check, prebuild, manifest key check, and debug SHA-1 extraction</name>
  <files>.env.example (verify only), android/ (generated build output, gitignored)</files>

  <read_first>
    - app.config.js (the source of the package name and the Maps plugin props)
    - .env (confirm GOOGLE_MAPS_API_KEY is present and starts with AIza)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (§ Common Pitfalls 3, 4, 5, 6)
    - .planning/phases/01-foundation-auth/01-VALIDATION.md (§ Manual-Only Verifications)
    - .planning/phases/01-foundation-auth/01-07-SUMMARY.md (expected full-suite test count)
  </read_first>

  <action>
Run these in order from `/Users/shawonkanji/Documents/projects/go-ride/go-ride-user-app`. Stop and report if any step fails — do not paper over a failure to reach the checkpoints.

1. **Automated gate:**
```bash
npx tsc --noEmit
npm test
```
Both must exit 0. `npm test` must report at least the suite count recorded in `01-07-SUMMARY.md`.

2. **Generate the native project:**
```bash
npx expo prebuild --platform android --clean
```
`android/` is gitignored — this is a regenerable build artifact, not source. If prebuild fails on a missing `expo-dev-client` or autolinking issue, report the exact error rather than hand-editing files under `android/`.

3. **Prove the Maps key reached the manifest** (this is the check that catches `01-RESEARCH.md` Pitfall 4):
```bash
grep -A1 "com.google.android.geo.API_KEY" android/app/src/main/AndroidManifest.xml
```
The `android:value` on the following line must be a real `AIza...` string. It must NOT be:
- the literal text `process.env.GOOGLE_MAPS_API_KEY` (would mean the config was static JSON — impossible here, but check anyway),
- empty, or
- absent entirely (the plugin silently *removes* the meta-data entry when the prop is `undefined`, so a missing entry means `.env` was not loaded).

Also confirm the package name landed:
```bash
grep -o 'package="[^"]*"' android/app/src/main/AndroidManifest.xml || grep -rn "applicationId" android/app/build.gradle
```
must show `com.goride.rider`.

4. **Extract the local debug keystore SHA-1** (needed by Task 2). `~/.android/debug.keystore` is auto-created by the first prebuild/build; if it does not exist yet, run `cd android && ./gradlew signingReport` first, which creates it:
```bash
keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep -A1 "SHA1:"
```
Fallback if that fails:
```bash
cd android && ./gradlew signingReport | grep -B2 -A6 "Variant: debug"
```
Record the SHA-1 (the `XX:XX:...` colon-separated hex string) verbatim — Task 2 needs it character-for-character.

5. **Determine the verification target for Task 3** (`01-RESEARCH.md` Pitfall 5 — do NOT assume a device exists):
```bash
adb devices
```
- If a device is listed as `device` (not `unauthorized`/`offline`): use it. Also run `adb reverse tcp:8080 tcp:8080` and note that `EXPO_PUBLIC_API_BASE_URL` must be `http://localhost:8080/api/v1` for a physical device — `10.0.2.2` is an emulator-only alias (Pitfall 6). Update `.env` accordingly for the test run and record what you changed.
- If NO device is listed: report that an emulator install is required and print the exact commands for the human, do not start the multi-hundred-MB download unattended:
```bash
sdkmanager --install "emulator" "system-images;android-35;google_apis;x86_64"
avdmanager create avd -n goride-test -k "system-images;android-35;google_apis;x86_64"
emulator -avd goride-test
```
  With an emulator, `EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:8080/api/v1` (the value already in `.env`) is correct.

6. Append the physical-device networking note to `.env.example` if it is not already there (it was added in plan 01-01 — verify, don't duplicate).

Report in the task output: the tsc/test results, the manifest key check result (masked: first 6 chars + length), the package name found, the SHA-1 string, and which verification target (physical device / needs emulator) applies.
  </action>

  <verify>
    <automated>cd /Users/shawonkanji/Documents/projects/go-ride/go-ride-user-app && npx tsc --noEmit && npm test && test -f android/app/src/main/AndroidManifest.xml && grep -q "com.google.android.geo.API_KEY" android/app/src/main/AndroidManifest.xml && grep -A1 "com.google.android.geo.API_KEY" android/app/src/main/AndroidManifest.xml | grep -q 'android:value="AIza' && ! grep -q 'process.env' android/app/src/main/AndroidManifest.xml && grep -q "com.goride.rider" android/app/build.gradle && keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep -q "SHA1:" && echo PHASE_GATE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 with no failing or skipped suites
    - `android/app/src/main/AndroidManifest.xml` exists after prebuild
    - That manifest contains a `com.google.android.geo.API_KEY` meta-data entry whose `android:value` starts with `AIza`
    - That manifest contains no occurrence of the substring `process.env`
    - `android/app/build.gradle` contains `com.goride.rider` as the `applicationId`
    - `keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android` prints a `SHA1:` line, and that fingerprint is recorded verbatim in the task output
    - The task output states explicitly whether `adb devices` found a usable device or whether an AVD install is required
  </acceptance_criteria>

  <done>Automated gate green, native project generated, the real Maps key verified present in the manifest, the debug SHA-1 extracted, and the Task-3 verification target identified.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Register the debug keystore SHA-1 against the Maps API key</name>
  <files>none (external: Google Cloud Console)</files>

  <read_first>
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (§ Common Pitfalls, Pitfall 3 — why the EAS-flavoured guidance in .planning/research/PITFALLS.md is stale for this project)
  </read_first>

  <action>
Automation was attempted and is genuinely unavailable here: `gcloud` is not installed on
this machine (`which gcloud` → not found), so the API-key restriction cannot be scripted.
Present the human with the exact values from Task 1 and pause.

Print, verbatim, for the human to copy:
- Package name: `com.goride.rider`
- SHA-1 fingerprint: the `SHA1:` value Task 1 extracted from `~/.android/debug.keystore`

Do NOT proceed to Task 3 until the human confirms the entry is saved — a build run before
the restriction propagates would produce a misleading "key rejected" result.
  </action>

  <what-built>
    Task 1 generated the Android project, confirmed the Maps API key value reached `AndroidManifest.xml`, and extracted this machine's debug-keystore SHA-1 fingerprint.
  </what-built>

  <why-human>
    The `gcloud` CLI is not installed on this machine, and API-key application restrictions cannot be changed without an authenticated Google Cloud session. This is genuinely not automatable here.
  </why-human>

  <how-to-verify>
    1. Open Google Cloud Console → **APIs &amp; Services → Credentials**, in the same project that already hosts `go-ride-driver-app`'s Maps key.
    2. Open the existing Maps API key (the one whose value is in `../go-ride-driver-app/.env` as `MAP_API_KEY`).
    3. Under **Application restrictions**, with **Android apps** selected, click **Add an item** and enter:
       - **Package name:** `com.goride.rider`
       - **SHA-1 certificate fingerprint:** the exact value Task 1 printed
    4. **Do not remove** the existing `com.goride.driver` entry — both apps share this one key, and deleting the driver app's entry would break it.
    5. Save. Google Cloud notes that restriction changes can take up to ~5 minutes to propagate.
    6. Confirm the API restrictions still include **Maps SDK for Android**.
  </how-to-verify>

  <resume-signal>Type "registered" once the com.goride.rider + SHA-1 entry is saved, or describe what you found instead (e.g. the key has no Android restriction at all, or a different key is in use).</resume-signal>

  <verify>
    <automated>echo "Human-gated: Google Cloud Console API-key restriction. gcloud CLI is not installed (verified 2026-08-11), so no automated assertion is possible. Task 3's successful build+run on a real device is the downstream automated proof."</automated>
  </verify>

  <acceptance_criteria>
    - The human confirmed an Android-apps restriction entry pairing package `com.goride.rider` with the exact SHA-1 printed by Task 1 is saved on the shared Maps API key
    - The pre-existing `com.goride.driver` entry was NOT removed
    - API restrictions on the key still include Maps SDK for Android
    - The SHA-1 registered is recorded verbatim in this plan's SUMMARY
  </acceptance_criteria>

  <done>The human has confirmed the com.goride.rider + debug SHA-1 pair is registered against the shared Maps API key, alongside the untouched driver-app entry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end AUTH-01..06 verification on a real Android build</name>
  <files>none (verification only)</files>

  <read_first>
    - .planning/phases/01-foundation-auth/01-VALIDATION.md (§ Manual-Only Verifications — this checkpoint is that table, executed)
    - .planning/ROADMAP.md (Phase 1 Success Criteria — the five statements being confirmed)
    - .env (confirm EXPO_PUBLIC_API_BASE_URL matches the target chosen in Task 1)
  </read_first>

  <action>
Everything automatable has already run in Task 1 (type-check, full Jest suite, prebuild,
manifest key verification). What remains genuinely cannot be automated: session persistence
across a real process kill, and a human confirming the six AUTH behaviours on a real screen.

Before pausing, do the automatable setup:
1. Confirm the backend is reachable: `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/auth/login -X POST` should return an HTTP status (any status proves it is listening), not a connection error.
2. Re-run `adb devices`. If a physical device is attached, run `adb reverse tcp:8080 tcp:8080` and set `EXPO_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1` in `.env`. If an AVD is running, leave `.env` at `http://10.0.2.2:8080/api/v1`.
3. Start the build: `npx expo run:android`. Wait for install and launch, then hand over.

Then pause for the human to walk the flow below.
  </action>

  <what-built>
    The complete Phase 1 rider app: signup, login, persisted session, profile view/edit, change password, logout, and the pre-expiry session warning — all backed by a green Jest suite and a native Android project whose Maps key is verified.
  </what-built>

  <how-to-verify>
    **Prerequisites**
    - `go-ride-backend` running locally on port 8080.
    - Target ready per Task 1: either a physical Android device with USB debugging (then `adb reverse tcp:8080 tcp:8080` and `EXPO_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1`), or a running AVD (then keep `EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:8080/api/v1`).

    **Build and install**
    ```
    npx expo run:android
    ```
    Expect a first build of several minutes. Watch for a successful install and app launch.

    **Walk the flow — confirm each numbered step:**
    1. App opens on the **Log in** screen (not Signup, not a blank screen). — AUTH-02 guard
    2. Tap **Sign up**, fill first name, last name, a new email, and a password of at least 8 characters. Submit. You land inside the app on the **Home** tab showing "Booking a ride is coming soon". — AUTH-01
    3. The tab bar shows exactly **two** tabs: Home and Profile. No Vehicles/Verify/Booking tab.
    4. **Force-quit** the app completely (swipe it away from recents — do not just background it) and reopen it. You are still logged in and land on Home, with no flash of the Login screen. — AUTH-02
    5. Open **Profile**. You see your first and last name, your email as plain text, and an **Active** status badge. There is no email-verification badge anywhere. — AUTH-03
    6. Tap **Edit profile**, change the first name, save. You return to Profile and the new name is displayed. — AUTH-03
    7. Back on Profile, tap **Change password**. Enter a wrong current password → you see a single error banner and stay on the screen. Now enter the correct current password and a new 8+ character password → you see a **Password changed** confirmation and you are still logged in (not bounced to Login). — AUTH-05
    8. Tap **Back to profile**, then **Log out**. You return to the **Log in** screen. — AUTH-04
    9. Log back in with the **new** password to confirm step 7 actually took effect.
    10. **Session expiry warning (AUTH-06):** the JWT expires 60 minutes after login, and the banner appears in the final 5 minutes. Either leave the app installed and reopen it ~55 minutes after logging in and confirm a "Session ending in N min" banner appears at the top of the authenticated screens, or — if the backend is under your control — temporarily set `JWT_EXPIRY_MINUTES=6` in the backend's env, restart it, log in fresh, and confirm the banner appears within about a minute. Confirm the banner is dismissible, and that logging out and back in makes it re-evaluate for the new token.

    **Report anything that differs from the above, including cosmetic surprises.**
  </how-to-verify>

  <resume-signal>Type "approved" if all 10 steps behaved as described, or list the step numbers that failed and what happened instead.</resume-signal>

  <verify>
    <automated>cd /Users/shawonkanji/Documents/projects/go-ride/go-ride-user-app && npx tsc --noEmit && npm test && adb shell pm list packages | grep -q "com.goride.rider" && echo "APP_INSTALLED_ON_TARGET"</automated>
  </verify>

  <acceptance_criteria>
    - `adb shell pm list packages` lists `com.goride.rider` — the build actually installed on the target
    - `npx tsc --noEmit` and `npm test` both exit 0
    - The human confirmed all 10 numbered steps, explicitly including step 4 (force-quit and reopen still logged in) and step 10 (session-expiry banner observed)
    - Any failing step is recorded by number in this plan's SUMMARY with what was observed instead
  </acceptance_criteria>

  <done>A human has walked all 10 steps on a real Android build and confirmed every ROADMAP Phase 1 success criterion, or has reported the specific steps that failed.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` and `npm test` both exit 0 (automated phase gate).
2. `grep -A1 "com.google.android.geo.API_KEY" android/app/src/main/AndroidManifest.xml` shows `android:value="AIza..."` — a real key, not a placeholder, not `process.env...`, not missing.
3. `android/app/build.gradle` shows `applicationId "com.goride.rider"`.
4. Human confirms the SHA-1 + `com.goride.rider` pair is saved in Google Cloud Console alongside the existing `com.goride.driver` entry.
5. Human confirms all 10 manual steps, covering every one of ROADMAP Phase 1's five success criteria.
</verification>

<success_criteria>
- All six AUTH requirements observed working on a real Android build, not just under mocks
- Session persistence proven the only way it can be: force-quit and reopen
- The Google Maps key path (`.env` → `app.config.js` → config plugin → `AndroidManifest.xml`) proven end to end, and the local debug SHA-1 registered — so Phase 3 can render a map without a blank-map debugging detour
- `.planning/STATE.md`'s "Google Maps API key must be verified against a real EAS production build" concern is resolved and corrected to the local-debug-build reality
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-08-SUMMARY.md`.
Record explicitly:
- the registered debug SHA-1 (safe to record — a fingerprint, not a secret) and the exact package name registered
- which verification target was used (physical device vs. AVD) and the `EXPO_PUBLIC_API_BASE_URL` value that worked, so the next verification pass does not rediscover Pitfall 6
- how AUTH-06 was verified (waited out the hour vs. shortened `JWT_EXPIRY_MINUTES`)
- any step that needed a fix, and whether it was fixed here or deferred

Also update `.planning/STATE.md` Blockers/Concerns to mark the Maps-key concern resolved, replacing the stale "EAS production build" wording with the local-debug-keystore reality.
</output>
