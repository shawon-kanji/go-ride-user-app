---
phase: 02-fare-estimate-booking
plan: 09
type: execute
wave: 9
depends_on: ["02-08"]
files_modified:
  - .planning/phases/02-fare-estimate-booking/02-09-SUMMARY.md
  - .planning/phases/02-fare-estimate-booking/02-VALIDATION.md
autonomous: false
requirements: [RIDE-01, RIDE-02]
user_setup: []

must_haves:
  truths:
    - "The full Jest suite, TypeScript type-check, and lint are all green at the phase gate"
    - "A rider on a real Android build can set pickup and dropoff on a real map and receive an itemized fare estimate"
    - "A rider sees the countdown ticking against the real backend's expires_at and, on expiry, is offered a manual re-quote rather than a silent refetch"
    - "A rider can book a cab and see the ride-requested confirmation"
    - "Rapidly double-tapping Book this ride produces exactly one trip_requests row in the backend database"
  artifacts:
    - path: ".planning/phases/02-fare-estimate-booking/02-09-SUMMARY.md"
      provides: "Recorded evidence for every ROADMAP Phase 2 success criterion"
      min_lines: 20
  key_links:
    - from: "src/features/booking/components/FareQuoteScreen.tsx"
      to: "cab-request-handler POST /request-cab"
      via: "a real 202 Accepted observed on device, creating exactly one trip_requests row"
      pattern: "request-cab"
---

<objective>
Close Phase 2 against reality: run the full automated gate, build and install on the physical Android device, and have a human walk the entire fare-estimate-to-booking journey against the real `cab-request-handler` — including the two things mocked tests genuinely cannot prove, namely a real map gesture flow and the exactly-once guarantee under a real double-tap.

Purpose: `02-VALIDATION.md` § Manual-Only Verifications lists precisely this. RIDE-02's whole reason for existing is "no duplicate trip", and the only proof that counts is a row count in the backend database after a deliberate double-tap. `01-08-SUMMARY.md` established the device-testing pattern for this machine; this plan reuses it rather than rediscovering it.
Output: A human-signed Phase 2, with evidence recorded per success criterion.
</objective>

<execution_context>
@/Users/shawonkanji/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawonkanji/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fare-estimate-booking/02-VALIDATION.md
@.planning/phases/02-fare-estimate-booking/02-CONTEXT.md
@.planning/phases/02-fare-estimate-booking/02-08-SUMMARY.md
@.planning/phases/02-fare-estimate-booking/02-02-SUMMARY.md
@.planning/phases/01-foundation-auth/01-08-SUMMARY.md
@.planning/ROADMAP.md

<environment_facts>
Established on this machine during Phase 1 plan 01-08 and re-confirmed in plan 02-02. Reuse, do not rediscover:
- Local builds only (`npx expo run:android`). No EAS.
- Physical target: Galaxy S21 over **Wi-Fi, not USB**. `EXPO_PUBLIC_API_BASE_URL` and `EXPO_PUBLIC_CAB_API_BASE_URL` must both use the laptop's LAN IP from `ipconfig getifaddr en0` (was `192.168.100.136`), on ports 8080 and 8082 respectively.
- **Port 8081 is often occupied by a local Go service.** `expo run:android` silently skips starting Metro when anything answers on 8081, and the dev client lands on `DevLauncherErrorActivity`. Workaround: `npx expo start --port 8090 --dev-client` and connect the dev client manually to `<LAN-IP>:8090`.
- Maps key + debug SHA-1 for `com.goride.rider` are already registered. No Cloud Console work here.
- Two backends must be running: `go-ride-backend` on 8080 (auth/profile) and `cab-request-handler` (from `go-ride-kafka-consumers`) on 8082.
</environment_facts>

<roadmap_success_criteria>
ROADMAP.md Phase 2 — the three statements this plan must produce evidence for:
1. Rider can select pickup/dropoff and receive a fare estimate with an itemized breakdown
2. Rider sees a visible countdown against the quote's expiry, and booking against an expired quote is never silently accepted
3. Rider can book a cab against the quote, and repeated taps or network retries of the same intent never create a duplicate trip
</roadmap_success_criteria>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase gate, device build, and backend reachability check</name>
  <files>android/ (regenerated build output, gitignored), .env (verify only)</files>

  <read_first>
    - .env (confirm BOTH base URLs are present and use the current LAN IP)
    - .planning/phases/02-fare-estimate-booking/02-VALIDATION.md (§ Sampling Rate, § Manual-Only Verifications)
    - .planning/phases/02-fare-estimate-booking/02-08-SUMMARY.md (the expected full-suite test/suite count entering this gate)
    - .planning/phases/01-foundation-auth/01-08-SUMMARY.md (§ Deviations — the Wi-Fi/LAN-IP and Metro-port workarounds)
  </read_first>

  <action>
Run in order from `/Users/shawonkanji/Documents/projects/go-ride/go-ride-user-app`. Stop and report on any failure — do not paper over one to reach the checkpoint.

**1. Automated gate:**
```bash
npx tsc --noEmit
npm test
npx expo lint
```
All three must exit 0. `npm test` must report at least the suite count recorded in `02-08-SUMMARY.md`.

**2. Confirm both backends are listening:**
```bash
curl -s -o /dev/null -w "backend-8080:%{http_code}\n" -X POST http://localhost:8080/api/v1/auth/login
curl -s -o /dev/null -w "cab-8082:%{http_code}\n"     -X POST http://localhost:8082/api/v1/cab/fare-estimate
```
Any HTTP status proves the port is listening; a connection error means the service is down. Both must respond.

**3. Confirm device networking config.** Get the current LAN IP and verify `.env`:
```bash
ipconfig getifaddr en0
grep EXPO_PUBLIC .env
```
`EXPO_PUBLIC_API_BASE_URL` must be `http://<LAN-IP>:8080/api/v1` and `EXPO_PUBLIC_CAB_API_BASE_URL` must be `http://<LAN-IP>:8082/api/v1/cab`, both with the CURRENT IP. Update `.env` if the IP has changed since plan 02-01 and record what you changed.

**4. Confirm the cab service is reachable from the LAN IP, not just localhost** — this catches a service bound to `127.0.0.1` only, which would work in `curl` above and fail from the phone:
```bash
curl -s -o /dev/null -w "cab-from-lan:%{http_code}\n" -X POST http://$(ipconfig getifaddr en0):8082/api/v1/cab/fare-estimate
```

**5. Build and install:**
```bash
adb devices
```
- If no device is listed as `device`: STOP and report — this plan needs hardware and must not start an emulator system-image download unattended.
- Check port 8081 (`lsof -nP -iTCP:8081 -sTCP:LISTEN`). If occupied, start Metro yourself first (`npx expo start --port 8090 --dev-client`) and note the URL for the dev client.
```bash
npx expo run:android
```

**6. Capture a pre-test baseline row count** so Task 2's duplicate check is a delta, not an absolute. Find the `trip_requests` table's current count for the test rider. Determine the DB connection from `go-ride-kafka-consumers/services/cab-request-handler/.env` (or `.env.example`) and run the equivalent of:
```sql
SELECT count(*) FROM trip_requests;
```
Record the number and the exact command used, so Task 2 can re-run it verbatim. If the DB is not reachable from this shell, say so explicitly — the human will need an alternative way to count rows and Task 2's instructions must be adjusted accordingly.

Report: the three gate results, both curl statuses, the LAN-IP curl status, the current `.env` values, whether the build installed, the Metro port used, and the baseline `trip_requests` count plus the exact command.
  </action>

  <verify>
    <automated>cd /Users/shawonkanji/Documents/projects/go-ride/go-ride-user-app && npx tsc --noEmit && npm test && npx expo lint && grep -q "EXPO_PUBLIC_CAB_API_BASE_URL" .env && adb shell pm list packages | grep -q "com.goride.rider" && echo PHASE_2_GATE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 with no failing and no skipped suites, reporting at least the count in `02-08-SUMMARY.md`
    - `npx expo lint` exits 0
    - Both curl checks return an HTTP status code (not a connection error) for ports 8080 and 8082
    - The LAN-IP curl to port 8082 also returns an HTTP status code — proving the cab service is not bound to localhost only
    - `.env` contains both `EXPO_PUBLIC_API_BASE_URL` and `EXPO_PUBLIC_CAB_API_BASE_URL`, both using the IP printed by `ipconfig getifaddr en0`
    - `adb shell pm list packages` lists `com.goride.rider`
    - The task output records the baseline `trip_requests` row count and the verbatim command used to obtain it (or an explicit statement that the DB was unreachable from this shell)
    - The task output records the Metro port used and whether the port-8081 workaround was needed
  </acceptance_criteria>

  <done>Automated gate green, both backends confirmed reachable from the LAN IP, the app installed on the physical device, and a baseline `trip_requests` count captured for the duplicate-booking check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end RIDE-01/RIDE-02 verification on a real Android build</name>
  <files>none (verification only)</files>

  <read_first>
    - .planning/phases/02-fare-estimate-booking/02-VALIDATION.md (§ Manual-Only Verifications, row 2 — this checkpoint IS that row, executed)
    - .planning/ROADMAP.md (Phase 2 Success Criteria — the three statements being confirmed)
    - .planning/phases/02-fare-estimate-booking/02-08-SUMMARY.md (the error-sentinel-to-UI mapping, for interpreting anything unexpected)
  </read_first>

  <action>
Everything automatable already ran in Task 1: type-check, full Jest suite, lint, both backend
reachability checks, the LAN-IP reachability check, the device build and install, and the
baseline `trip_requests` row count. What remains genuinely cannot be automated — see
`<why-human>`.

Before pausing, do the automatable setup so the human only has to walk the flow:
1. Re-confirm the install: `adb shell pm list packages | grep com.goride.rider`.
2. Re-confirm Metro is serving on the port Task 1 reported:
   `curl -s http://localhost:<metro-port>/status` should print `packager-status:running`.
   If not, restart it (`npx expo start --port 8090 --dev-client`) and report the URL the dev
   client must connect to.
3. Launch the app: `adb shell monkey -p com.goride.rider -c android.intent.category.LAUNCHER 1`.
4. Print, verbatim, for the human to use in step 15: the baseline `trip_requests` count from
   Task 1 and the exact command that produced it. If the DB was unreachable from this shell,
   say so plainly and instead start streaming `cab-request-handler`'s logs so the burst of
   `/request-cab` calls in step 14 can be counted from there.
5. Clear the device log immediately before handing over (`adb logcat -c`) so any error the
   human hits can be captured cleanly afterwards with
   `adb logcat -d | tail -100`.

Then pause for the human to walk the 16 steps below. When they respond, re-run the row-count
command from step 4 yourself and report the delta rather than relying on a recalled number.
  </action>

  <what-built>
    The complete Phase 2 booking flow on a real device: a Book tab leading to a two-step tap-to-pin map (GPS-defaulted pickup, manual dropoff), a fare quote screen with an itemized breakdown and a live 15-minute countdown, an explicit expired state with a manual re-quote, and idempotent booking against `cab-request-handler` — all behind a green Jest suite, type-check, and lint.
  </what-built>

  <why-human>
    Three things cannot be proven by mocked tests. Real map gestures and the real GPS permission dialog only exist on hardware. The real `expires_at` value and its countdown only come from the live backend. And RIDE-02's entire claim — "repeated taps never create a duplicate trip" — is a statement about rows in the backend database under a genuine fast double-tap, which no client-side test can assert.
  </why-human>

  <how-to-verify>
    **Prerequisites**
    - `go-ride-backend` running on 8080 and `cab-request-handler` running on 8082, both reachable from the phone's Wi-Fi network (Task 1 confirmed this).
    - The app freshly installed by Task 1 and connected to Metro on the port Task 1 reported.

    **Walk the flow — confirm each numbered step:**

    1. Log in (or reopen an existing session). The tab bar shows exactly **three** tabs: Home, Book, Profile.
    2. On **Home**, you see a "Book a ride" card with a **Book a ride** button. The old "Booking a ride is coming soon" text is gone.
    3. Tap **Book a ride**. A location permission dialog appears ("Allow GoRide to access this device's location?"). **Grant it.**
    4. The map opens centred on roughly your actual location, with a pin already placed there, under a **Set pickup** heading. A `Pickup: <lat>, <lng>` label shows four-decimal coordinates.
    5. **Tap elsewhere on the map.** The pin moves to where you tapped and the label updates. **Pinch to zoom and drag.** The viewport stays where you left it — it must NOT snap back when you move the pin. — RIDE-01
    6. Tap **Confirm pickup**. The heading becomes **Set dropoff** and there is now NO pin on the map. Tap somewhere a few kilometres away to place the dropoff, then tap **Confirm dropoff**.
    7. The **Fare estimate** screen appears. Confirm it shows rows for **Base fare**, **Distance**, **Time**, and a prominent **Total**, each as `<CURRENCY> <amount>` with two decimals. Confirm there is **no** Surcharge row, **no** Discount row, and **nothing** mentioning surge or a multiplier. — ROADMAP criterion 1
    8. A countdown reads **Quote expires in MM:SS**, starting near `15:00`, and visibly ticks down every second. — ROADMAP criterion 2
    9. **Background the app** (home button) for about a minute, then reopen it. The countdown must show the correctly-elapsed time, not the value it had when you left. — the anti-drift guarantee
    10. **Expiry path.** Either wait out the full 15 minutes, or (faster, if you control the service) restart `cab-request-handler` with `FARE_LOCK_TTL_MINUTES=1` and get a fresh estimate. When it reaches zero: the countdown reads **Quote expired**, the **Book this ride** button is GONE, and a single **Get new estimate** button appears. Confirm the app did NOT quietly fetch a new quote on its own — the total should not have changed by itself. — ROADMAP criterion 2
    11. Tap **Get new estimate**. A fresh quote appears with a reset countdown, and you were NOT sent back to the map to re-pick locations.
    12. **Book normally.** Tap **Book this ride** once. Within a moment you see a **Ride requested!** confirmation and a **Back to home** button. Confirm there is NO "finding driver", no spinner-searching state, and no live map — that is deliberately Phase 3. — ROADMAP criterion 3
    13. Tap **Back to home**. You land on the Home tab.
    14. **The duplicate-booking test — the most important step.** Book a completely fresh ride: Book a ride → set pickup → set dropoff → on the quote screen, **tap "Book this ride" as fast as you can, three or four times in a row.** You should still land on exactly one **Ride requested!** confirmation, with no error banner.
    15. Report the resulting `trip_requests` row count so it can be compared against Task 1's baseline. Steps 12 and 14 together should have added **exactly 2** rows, not 3 or 5. (Task 1 recorded the exact command to run; if the DB was unreachable there, report instead what the app showed and any `cab-request-handler` log lines for the burst of requests.)
    16. **Airplane-mode retry (optional but valuable).** Get a fresh quote, turn Wi-Fi off, tap **Book this ride** → you should see `Something went wrong, please try again.` and the button should still be available. Turn Wi-Fi back on and tap **Book this ride** again → you should get **Ride requested!**. Confirm this added only **one** row, not two — the retry reused the same idempotency key.

    Report anything that differs from the above, including cosmetic surprises. In particular, note the literal text of any error banner you did not expect.
  </how-to-verify>

  <resume-signal>Type "approved" if steps 1-15 behaved as described (note separately whether you ran step 16), or list the step numbers that failed and exactly what you saw instead — including the observed `trip_requests` delta if it was not 2.</resume-signal>

  <verify>
    <automated>cd /Users/shawonkanji/Documents/projects/go-ride/go-ride-user-app && npx tsc --noEmit && npm test && npx expo lint && adb shell pm list packages | grep -q "com.goride.rider" && echo "BUILD_PRESENT — map gestures, real countdown, and the trip_requests row-count delta are human-gated; no automated assertion is possible for those"</automated>
  </verify>

  <acceptance_criteria>
    - The human confirmed all three ROADMAP Phase 2 success criteria by their step numbers: criterion 1 (steps 4-7), criterion 2 (steps 8-11), criterion 3 (steps 12, 14, 15)
    - The human reported the actual `trip_requests` row-count DELTA across steps 12 and 14, and it is exactly `2` — or, if it is not, that number is recorded verbatim and the phase is NOT signed off
    - The human confirmed the fare breakdown showed no Surcharge row, no Discount row, and nothing surge-related
    - The human confirmed that at countdown zero the Book button disappeared and no automatic re-quote occurred
    - The human confirmed the post-booking confirmation contained no "finding driver" or live-tracking UI
    - The human confirmed the map viewport did not snap back when the pin was moved (step 5)
    - The human reported what the countdown showed after backgrounding for a minute (step 9)
    - Every failing step is recorded by number in this plan's SUMMARY with what was observed instead
    - `npx tsc --noEmit`, `npm test`, and `npx expo lint` all exit 0
  </acceptance_criteria>

  <done>A human has walked the full booking journey on real hardware against both real backends, and the `trip_requests` row count proves RIDE-02's exactly-once guarantee — or has reported the specific steps and row counts that failed.</done>
</task>

<task type="auto">
  <name>Task 3: Close out the validation contract and project state</name>
  <files>.planning/phases/02-fare-estimate-booking/02-VALIDATION.md</files>

  <read_first>
    - .planning/phases/02-fare-estimate-booking/02-VALIDATION.md (the Per-Task Verification Map and Wave 0 Requirements checklists to tick)
    - .planning/STATE.md (§ Blockers/Concerns — the Places/Routes API entry to resolve)
    - .planning/REQUIREMENTS.md (§ Traceability — the RIDE-01/RIDE-02 rows to mark)
  </read_first>

  <action>
Only run this task after Task 2 is approved.

**1. `.planning/phases/02-fare-estimate-booking/02-VALIDATION.md`:**
- Set frontmatter `status: complete` and `wave_0_complete: true`.
- In the **Per-Task Verification Map**, change every row's Status from `⬜ pending` to `✅ green` (or `❌ red` with a note for anything that failed).
- Tick every box in **Wave 0 Requirements**.
- Tick every box in **Validation Sign-Off** and change **Approval** from `pending` to the date and outcome.

**2. `.planning/STATE.md`** — resolve the Phase 2 blocker entry. The current text reads:
> Phase 2 (Fare Estimate & Booking) has MEDIUM-confidence gaps: exact Places API (New) / Routes API request/response shapes not yet exercised in this codebase — worth a focused check before finalizing the booking-screen data model.

Strike it through and append the resolution: Places/Routes API were never needed — `02-CONTEXT.md` locked map-tap-to-pin, which uses only raw lat/lng against `POST /fare-estimate`, so no Places integration exists in v1 and text search remains a v2 deferral.

Also add to Decisions:
- `session-store.hydrate()` restores only the token, never `user` — any feature needing `User.id` must use plan 02-04's `useRiderId` fallback pattern (this was found during planning, not at runtime).
- The two-route `book/` flow (`index` picker with internal step state → `quote`) with a zustand draft store carrying coordinates, rather than route params.

**3. `.planning/REQUIREMENTS.md`** — mark RIDE-01 and RIDE-02 complete in both the Ride Booking checklist and the Traceability table.

**4. `.planning/ROADMAP.md`** — tick Phase 2's checkbox in the Phases list and update the Progress table row to `9/9` / `Complete` with today's date.
  </action>

  <verify>
    <automated>cd /Users/shawonkanji/Documents/projects/go-ride/go-ride-user-app && grep -q "status: complete" .planning/phases/02-fare-estimate-booking/02-VALIDATION.md && ! grep -q "⬜ pending" .planning/phases/02-fare-estimate-booking/02-VALIDATION.md && grep -q "\[x\] \*\*RIDE-01\*\*" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*RIDE-02\*\*" .planning/REQUIREMENTS.md && grep -q "useRiderId" .planning/STATE.md && npm test && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `02-VALIDATION.md` frontmatter has `status: complete` and `wave_0_complete: true`
    - `02-VALIDATION.md` contains no remaining `⬜ pending` or `TBD` strings
    - `.planning/REQUIREMENTS.md` shows `- [x] **RIDE-01**` and `- [x] **RIDE-02**`, and both Traceability rows read `Complete`
    - `.planning/STATE.md` no longer presents the Places/Routes API gap as an open concern, and records both new decisions (the `useRiderId` cold-start finding and the two-route booking flow)
    - `.planning/ROADMAP.md` Phase 2 checkbox is ticked and its Progress row reads `9/9` / `Complete`
    - `npm test` and `npx tsc --noEmit` exit 0
  </acceptance_criteria>

  <done>The validation contract is signed off, the Phase 2 blocker is resolved, both requirements are marked complete, and the roadmap reflects a finished phase.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit`, `npm test`, and `npx expo lint` all exit 0.
2. `adb shell pm list packages` lists `com.goride.rider`.
3. A human confirms all three ROADMAP Phase 2 success criteria on the device, including a `trip_requests` delta of exactly 2 across the normal booking and the deliberate double-tap booking.
4. `02-VALIDATION.md` has no remaining `⬜ pending` or `TBD` entries; `REQUIREMENTS.md` marks RIDE-01 and RIDE-02 complete.
</verification>

<success_criteria>
- Both RIDE-01 and RIDE-02 are observed working on real hardware against the real `cab-request-handler`, not just under mocks
- RIDE-02's exactly-once claim is proven the only way it can be: a row count in the backend after a deliberate fast double-tap
- The expiry guarantee is proven by watching a real quote expire and seeing a manual re-quote offered rather than a silent refetch
- Phase 3 inherits a booking that actually creates a trip request, which is its entire starting precondition
</success_criteria>

<output>
After completion, create `.planning/phases/02-fare-estimate-booking/02-09-SUMMARY.md`.
Record explicitly:
- the observed `trip_requests` baseline, the post-walkthrough count, and the delta — this is the primary evidence for RIDE-02
- what the countdown showed after the step-9 backgrounding test
- how expiry was exercised (waited out 15 minutes vs. a shortened `FARE_LOCK_TTL_MINUTES`)
- whether the airplane-mode retry (step 16) was run and what row delta it produced
- the `EXPO_PUBLIC_CAB_API_BASE_URL` value that actually worked from the device, and the Metro port used
- any step that needed a fix, and whether it was fixed here or deferred
- what Phase 3 inherits: the `trip_id`/`request_id` shape returned by a real 202, since Phase 3's `/current-trip` reconciliation starts from it
</output>
